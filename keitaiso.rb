#encoding: utf-8

#実行する前にコマンドプロンプトで以下のように環境変数を指定すること！
#$ set LANG=ja_JP.UTF-8

require 'csv'

CSV.foreach('Tweet_out2.csv') do |str|

#B列に'名詞'、C列に'一般'と書かれ、G列に'*'が入っていない行のA列の文字列を返す
#つまりこの場合は、純粋な名詞と判断された単語のみを抜き出す
#Mecabでは'ー'なども名詞と判断された．その場合，G列に'*'が入るのを利用して純粋名詞のみ抜き出す必要があった
     if str[1] == '名詞' && str[2]=='一般' && str[7] !="*"  then
     #固有名詞もあったので，固有名詞のみ抜き出してテキストに追記
     #if str[1] == '名詞' && str[2]=='固有名詞' && str[7] !="*" then
        puts str[0]
     end    
end



